# Changesets

This directory contains release notes consumed by `@changesets/cli`.

Create a changeset with `npx changeset`, then apply pending version bumps and changelog entries with `npx changeset version`.
